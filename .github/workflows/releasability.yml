name: Releasability status
on:
  workflow_dispatch:
  workflow_run:
    workflows: [Build]
    types:
      - completed
    branches:
      - master

jobs:
  update_releasability_status:
    runs-on: github-ubuntu-latest-s
    name: Releasability status
    permissions:
      id-token: write
      statuses: write
      contents: read
    steps:
      - uses: SonarSource/gh-action_releasability/releasability-status@f6c1978d7b7642e57846be4823bb28ec9e7ff39b # 3.1.0
        with:
          optional_checks: "Jira,CheckDependencies,CheckManifestValues"
        env:
          GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}'
