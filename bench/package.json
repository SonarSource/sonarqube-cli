{
  "name": "stats-storage-bench",
  "type": "module",
  "private": true,
  "description": "Throwaway benchmark comparing SQLite+JSON1, LMDB, and NDJSON for the sonar stats storage design (MMF-5834)",
  "scripts": {
    "bench": "bun run run.ts"
  },
  "dependencies": {
    "lmdb": "^3.5.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
